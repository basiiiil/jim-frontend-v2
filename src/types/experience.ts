export type ExperienceType = {
  id?: string;
  title?: string; // experience title
  description?: string; // degree description
  type?: string; // type of Experience
  specialty?: string; // if elective/course, choose specialty
};
